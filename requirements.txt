python-binance
textual
rich
pandas
python-dotenv
numpy
aiohttp

# Telegram Bot (uses aiohttp directly — no extra deps)

# Render deployment (FastAPI web server)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
matplotlib>=3.8.0
google-genai>=1.0.0
edge-tts>=6.0